memodir = "$HOME/memo"
editor = "vim"
column = 35
selectcmd = "fzf"
grepcmd = "rg --max-depth 1 --sortr path ${PATTERN} ${DIR}"
memotemplate = "$HOME/.config/memo/template.txt"
pluginsdir = "$HOME/.config/memo/plugins"

