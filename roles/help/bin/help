#!/usr/bin/env zsh

# 自分がよく忘れることを書いておいて、サッと見れるための help
# 中身は何でも良い。特に決めてない。よく使うコマンド、忘れがちなコマンドとか。


main() {
cat << 'EOF'
# Cheat
- finch で docker を動かす
  finch run -it --rm public.ecr.aws/debian/debian:stable-slim /bin/bash
- git commit id を指定して rebase する。
  git rebase -i $(gci-rebase)
- convert HEIC to jpg
  fd -e HEIC -d1 | while read line; do echo $line; convert $line -quality 80 $line.jpg; done
- markdown detail
  <details><summary>XXX</summary>\nXXX\n</details>
- コマンドで GitHub PR を作成する
  gh pr create -a '@me' -t '<Title>'


# Commands
- gci: git commit id を表示する（fzf で絞り込める）。
- gcd: git リポジトリの top レベルに移動する。
- gbr: git-branch-list
- gpn: github-pull-request-no
- git-unstage: 'git reset HEAD'
- k: kubectl
- repo: src 直下を fzf し、選択したら cd
- zi: 高アクセス頻度dir を fzf し、選択したら cd
- aws-nuke: aws nuke
- gci-rebase: rebase 用の git commit id（選んだ一つ前）を出力する
- ssh-add-keys: ~/.ssh/*.pem ファイルを ssh-add する
- tmux-resize-pane: tmux の pane サイズを変更する
EOF
}

main
